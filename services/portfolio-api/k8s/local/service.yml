apiVersion: v1
kind: Service
metadata:
  name: portfolio-api-service
spec:
  selector:
    app: portfolio-api
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8080
      nodePort: 30081
  type: NodePort
